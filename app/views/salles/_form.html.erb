<%= form_with(model: salle, local: true) do |form| %>
  <% if salle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(salle.errors.count, "error") %> prohibited this salle from being saved:</h2>

      <ul>
      <% salle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :courses %>
    <%= form.text_area :courses %>
  </div>

  <div class="field">
    <%= form.label :start_time %>
    <%= form.datetime_select :start_time %>
  </div>

  <div class="field">
    <%= form.label :end_time %>
    <%= form.datetime_select :end_time %>
  </div>

  <div class="field">
    <%= form.fields_for :materials do |f| %>
      <div class="field">
        <h4> Réserver un materiel <em> (facultatif) </em>  </h4>
        <%= f.text_field :name %>
      </div>
    <% end %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
